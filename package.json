{
  "name": "harbury-project-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Harbury Builders Club starter — Cloudflare Workers + Static Assets + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
